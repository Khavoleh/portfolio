---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<div class="mb-8">
  <h2 class="text-primary mb-4 text-3xl font-bold md:text-4xl">{title}</h2>
  <p class="text-secondary text-lg leading-relaxed">{description}</p>
</div>
