---
import { useI18n } from '@shared/helpers';
import { LAST_UPDATED_TRANSLATIONS } from './last-updated-translations';

const t = useI18n(Astro.url, LAST_UPDATED_TRANSLATIONS);
---

<p class="text-base-content/80 mb-6" set:html={t('terms.updated')} />
