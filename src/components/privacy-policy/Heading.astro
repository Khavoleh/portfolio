---
import { useI18n } from '@shared/helpers';
import { PRIVACY_POLICY_TRANSLATIONS } from './translations';

const t = useI18n(Astro.url, PRIVACY_POLICY_TRANSLATIONS);
---

<h1 class="text-base-content mb-8 text-4xl font-bold">{t('privacy.title')}</h1>
