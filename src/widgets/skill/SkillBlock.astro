---
import type { Skill } from './skill';

interface Props {
  skill: Skill;
}

const { skill } = Astro.props;
---

<div class="badge badge-lg badge-base gap-2">
  <span class="text-sm font-semibold">{skill.name}</span>
</div>
