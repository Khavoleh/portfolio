---
import '../styles/global.css';
import { ClientRouter } from 'astro:transitions';
import Analytics from '@vercel/analytics/astro';
import SpeedInsights from '@vercel/speed-insights/astro';
import DrawerSidebar from './DrawerSidebar.astro';
import Footer from './Footer.astro';
import { getCurrentLang } from './helpers.ts';
import Navbar from './Navbar.astro';
import SEO from './SEO.astro';
import languageInitScript from './scripts/language-init.js?raw';
import themeInitScript from './scripts/theme-init.js?raw';

export interface Props {
  title: string;
  description: string;
  noindex?: boolean;
}

const { title, description, noindex } = Astro.props;
---

<html lang={getCurrentLang(Astro.url)}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <SEO title={title} description={description} noindex={noindex} />
    <ClientRouter />
    <script is:inline set:html={themeInitScript} />
    <script is:inline set:html={languageInitScript} />
  </head>
  <body class="bg-base-100 text-base-content">
    <div class="drawer">
      <input id="mobile-drawer" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content flex min-h-screen flex-col">
        <Navbar />
        <main class="grow pt-20 pb-2">
          <slot />
        </main>
        <Footer />
      </div>
      <DrawerSidebar />
    </div>
  </body>
</html>

<Analytics />
<SpeedInsights />
