---
import { PAGE_URLS } from '@shared/constants.ts';
import { getLanguageUrl, useTranslations } from '@shared/helpers';
import type { SocialLink } from '@shared/interfaces.ts';
import { SOCIAL_LINKS } from './constants.ts';
import { FOOTER_TRANSLATIONS } from './translations.ts';

const t = useTranslations(Astro.url, FOOTER_TRANSLATIONS);
const currentYear = new Date().getFullYear();
---

<footer class="border-base-300 bg-base-100 border-t">
  <div class="mx-auto flex w-full max-w-7xl flex-col items-center justify-between gap-4 p-4 md:flex-row lg:py-6">
    <span class="text-base-content/70 text-center text-sm md:text-left">
      © {currentYear}
      {t('copyright')}
    </span>

    <nav class="flex flex-wrap items-center justify-center gap-4 text-sm font-medium md:gap-6">
      <a
        href={getLanguageUrl(Astro.url, PAGE_URLS.PRIVACY_POLICY)}
        class="focus-ring text-base-content/70 hover:text-base-content rounded"
      >
        {t('privacy.policy')}
      </a>
      <a
        href={getLanguageUrl(Astro.url, PAGE_URLS.TERMS_AND_CONDITIONS)}
        class="focus-ring text-base-content/70 hover:text-base-content rounded"
      >
        {t('terms.and.conditions')}
      </a>
    </nav>

    <nav class="flex justify-center gap-5">
      {
        SOCIAL_LINKS.map((link: SocialLink) => (
          <a
            href={link.href}
            class="focus-ring text-base-content/70 hover:text-base-content rounded"
            aria-label={`${t('navigate.to')} ${link.label}`}
            target="_blank"
            rel="noopener noreferrer"
          >
            <link.icon class="h-6 w-6" />
          </a>
        ))
      }
    </nav>
  </div>
</footer>
